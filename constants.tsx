
import { Product, ShopInfo } from './types';

export const INITIAL_SHOP_INFO: ShopInfo = {
  name: "Apexel Enterprise",
  manager: "Russell Siwale",
  workersCount: 0
};

// Starting with an empty inventory as requested
export const INITIAL_PRODUCTS: Product[] = [];
